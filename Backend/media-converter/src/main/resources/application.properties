spring.application.name=image-converter
# Allow large file uploads (adjust sizes as needed)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# Default: try whatever ?ffmpeg? is on the PATH.
video.ffmpeg.path=ffmpeg
